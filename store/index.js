/*
  Load
  State managment for load
*/

// Constant to reset this state information.
const initialState = () => ({})

// State object.
const state = initialState

// Getter functions.
const getters = {}

// Actions.
const actions = {}

// Mutations
const mutations = {}

export default {
  state,
  getters,
  actions,
  mutations
}
